
{
  "name": "student-community-app",
  "private": true,
  "scripts": {
    "dev": "npm-run-all --parallel dev:*",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
